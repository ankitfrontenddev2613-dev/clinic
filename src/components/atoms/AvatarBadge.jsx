import { StyleSheet, Text, View } from 'react-native'

const AvatarBadge = () => {
  return (
    <View>
      <Text>AvatarBadge</Text>
    </View>
  )
}

export default AvatarBadge

const styles = StyleSheet.create({})